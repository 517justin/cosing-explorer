.venv/
__pycache__/
*.pyc
# Rebuildable from the read-only CSV via ./run_all.sh
_data/kb.duckdb
# Large external downloads (Phase 4)
_data/lotus/*
!_data/lotus/.gitkeep

# GBIF response cache (rebuildable; keep for offline reruns)
!_data/gbif_cache.json

# npm (docx 產生器用)
node_modules/
package-lock.json
package.json

# 現行法規層（獨立於 2019 唯讀快照）
_data/cosing_2026/*.json

# 全量 inventory（若抓取，體積大）
_data/cosing_2026/inventory.csv

# LOTUS 凍結資料集（20MB，可自 Zenodo 重新下載）
_data/lotus/*.csv.gz
# PubChem 快取（可重建，但重跑要 40 分鐘，故保留）

# 原始 SVG 結構式（1.3GB，由 PubChem 下載，docs/svg/ 為壓縮後版本）
_data/structures/
