---
tags: [專案/知識圖譜]
status: 規劃中
created: 2026-08-11
---

# 植物萃取物知識圖譜 — 實作計劃

## 一、專案概述

把一萬筆植物萃取物的 CSV 資料,轉換成一個可瀏覽、可查詢、可持續累積的知識庫,並在其上建立知識圖譜。

**兩個目標:**

1. 發現資料中未被注意到的關聯
2. 作為 AI 問答的知識底層

**核心策略:** 資料本身沒有化學結構,但有生物來源。利用「親緣相近的植物產生相似次級代謝物」這個化學分類學原理,把物種分類階層當作化學相似度的骨幹;再透過外部開放資料庫把化學層補回來。

---

## 二、資料現況

| 項目 | 狀況 |
| --- | --- |
| 筆數 | 約 10,000 |
| 主體 | 植物萃取物,多為混合物 |
| 結構識別碼 | 無(無 SMILES / InChI) |
| 成分組成 | 無 |
| 含量比例 | 無 |
| 敘述欄位 | 有,含生物來源資訊(如「自某植物萃取」) |
| 結構化欄位 | 有分類 / 標籤 / 用途 |

**關鍵前提:** 敘述欄位中的植物名稱品質,決定整個專案的上限。Phase 1 若做不好,後面全部無效。

---

## 三、本體設計

### 節點類型

| 節點 | 數量級 | 來源 |
| --- | --- | --- |
| 萃取物 | 10,000 | CSV 原始列 |
| 物種學名 | 數百 ~ 2,000 | 從敘述抽取後正規化 |
| 屬 / 科 | 數十 ~ 數百 | GBIF 分類階層 |
| 使用部位 | < 20 | 從敘述抽取 |
| 萃取方法 | < 20 | 從敘述抽取 |
| 用途分類 | 數十 | CSV 既有欄位 |
| 已知成分 | 數千 | LOTUS 外部補充 |

### 關係類型

```
萃取物 --derived_from--> 物種學名
萃取物 --uses_part--> 使用部位
萃取物 --extracted_by--> 萃取方法
萃取物 --categorized_as--> 用途分類
物種學名 --belongs_to--> 屬 --belongs_to--> 科
物種學名 --contains--> 已知成分
```

### 設計原則

- **一萬筆萃取物不生成一萬則筆記。** 筆記寫在物種、科、用途、部位這些層級;萃取物本身留在資料庫,用查詢存取。
- 圖譜的價值來自「科屬階層」與「用途分類」的疊加,而非萃取物之間的直接連線。
- 所有外部補充的資料標明來源與擷取日期,與人工撰寫的內容分區塊存放。

---

## 四、環境準備

### 目錄結構

```
vault/
├── CLAUDE.md              # 給 Claude Code 的規範(見附錄)
├── _data/
│   ├── raw.csv            # 原始資料,唯讀
│   ├── kb.duckdb          # 萃取物主表 + 關聯表
│   └── lotus/             # LOTUS 下載的 TSV
├── _scripts/              # 所有處理腳本
├── 00-Inbox/
├── 10-Species/            # 物種筆記
├── 20-Family/             # 科筆記(本專案最有價值的部分)
├── 30-MOC/                # 用途、部位、萃取方法的樞紐頁
└── 90-Reports/            # 分析產出
```

### Claude Code 接法

日常操作直接 `cd` 進 vault 使用即可。需要在 Obsidian 執行中即時互動時,再走 MCP:

1. 安裝社群外掛 **Local REST API**,複製 API Key
2. 註冊 MCP server:

```bash
claude mcp add --transport http obsidian \
  https://127.0.0.1:27124/mcp/ \
  --header "Authorization: Bearer <API-KEY>" \
  --scope user
```

### 版本控制

vault 必須 `git init`。每個 Phase 開始前 commit,任何批次改寫前 commit。這不是建議事項。

---

## 五、實作階段

### Phase 0 — 資料剖析(0.5 天)

**目標:** 在動任何一行資料之前,先知道自己面對什麼。

- [ ] 每個欄位的型別、唯一值數量、空值率、長度分布
- [ ] 敘述欄位的寫法分群:抽樣 100 筆,歸納出生物來源的表達句型有幾種
- [ ] 統計敘述中出現的植物名候選詞頻
- [ ] 檢查重複列(同物種 + 同部位 + 同方法可能重複收錄)

**產出:** `90-Reports/data-profile.md`

**驗收:** 能回答「敘述裡的植物名有幾種寫法」「大概涉及多少個物種」。

---

### Phase 1 — 學名正規化(2–3 天)⭐ 成敗關鍵

**目標:** 把敘述中的植物名收斂成一張 `別名 → 接受學名` 對照表。

**流程:**

1. **抽取** — 用 LLM 從敘述中抽出植物名候選。這是 LLM 在本專案唯一真正該用的地方。
2. **提出候選學名** — 中文俗名交由 LLM 提出學名候選,允許多個。
3. **驗證** — 拿候選打 GBIF species match API(免費、無需金鑰、支援模糊比對)。模型幻覺出的假學名會在這關被擋掉。
4. **取接受名** — 使用 GBIF 回傳的 accepted name,不要用原始名。植物同物異名極普遍,不統一會讓同一物種在圖上裂成多個節點。
5. **人工審核** — 依出現頻次排序,審核前 200 名。長尾用規則處理。

**產出:** `_data/species_alias.csv`(欄位:原始字串、canonical_name、gbif_key、confidence、是否人工確認)

**驗收:** 前 200 高頻項目 100% 人工確認;整體匹配率 > 85%。

> [!warning] 中文俗名歧義
> 一名多物種的情況極多(「金銀花」「土茯苓」「白頭翁」在不同產區指涉不同物種)。GBIF 對中文俗名支援很弱。高頻項目必須人工判斷,無法確定的標記為 `ambiguous` 另外處理,不要硬猜。

---

### Phase 2 — 接上分類階層(0.5 天)

**目標:** 為每個物種取得完整分類階層。

- [ ] 從 GBIF match 結果取出 family / genus / order
- [ ] 建立 species → genus → family 的邊
- [ ] 統計各科的萃取物數量分布

**產出:** `_data/taxonomy.csv`

**驗收:** 每個已確認物種都有 family;無 family 的比例 < 5%。

---

### Phase 3 — 萃取條件抽取(0.5 天)

**目標:** 從敘述抽出使用部位與萃取方法。

這兩者詞彙固定(部位:葉、根、莖、皮、花、果、種子、全株;方法:水萃、醇萃、超臨界 CO₂、水蒸氣蒸餾、壓榨),**用規則比對就夠,不需要 LLM**。先建詞表,regex 或關鍵字比對,無法判定的留空。

**產出:** 萃取物主表新增 `plant_part`、`extraction_method` 欄位

> [!note] 為什麼這步重要
> 同一物種的不同部位,成分譜可能天差地遠。不區分部位的圖譜會把不相干的東西連在一起。

---

### Phase 4 — LOTUS 化學層富集(1 天)

**目標:** 把化學結構層補回來。

LOTUS 是開放的天然物資料庫,收錄 75 萬組以上經文獻引用的「結構—生物來源」配對,資料託管於 Wikidata,並鏡像於 `lotus.naturalproducts.net`。可下載為 TSV:化合物表(含 SMILES、InChIKey)、物種表、配對表。

- [ ] 下載 LOTUS TSV 至 `_data/lotus/`
- [ ] 以接受學名 join,得到每個物種的已知成分清單
- [ ] 記錄覆蓋率:哪些物種查得到、哪些查不到
- [ ] (可選)有了 SMILES 後,可用 RDKit 對成分做 Murcko 骨架分解與 Morgan 指紋相似度

**產出:** `_data/species_compounds.csv` + 覆蓋率報告

> [!warning] 覆蓋率偏誤
> **LOTUS 查不到 ≠ 該植物沒有成分**,只代表文獻未收錄。冷門物種覆蓋率會很低。分析時必須把「無資料」與「無成分」分開處理,否則所有結論都會偏向被研究得多的植物。

---

### Phase 5 — 挖掘隱藏關聯(1–2 天)

依行動價值排序:

**1. 空白格推薦** ⭐ 最有價值

統計某個用途的萃取物集中在哪幾個科,列出該科中資料庫尚未收錄的物種。產出一份可直接執行的候選清單。

**2. 用途相同但親緣極遠**

趨同演化的真實案例,或分類欄位標錯。兩種都值得逐一查核。

**3. 同科不同屬但用途一致**

化學分類學支持的關聯,可信度最高,適合作為推論依據。

**4. 同物種的橫向比較群組**

同一物種不同部位 / 不同萃取方法的組合,自動成組。

**5. 成分集合相似度**(需 Phase 4 完成)

以 LOTUS 成分集合計算萃取物之間的 Jaccard 相似度。只保留 top-k 鄰居(k = 5~10),不要建全連接圖。

**產出:** `90-Reports/` 下的各項分析筆記

---

### Phase 6 — Vault 生成(1 天)

**目標:** 用腳本生成筆記,不要讓 LLM 一則一則寫。

- [ ] 物種筆記(數百~兩千則):frontmatter 帶學名、GBIF key、屬、科;正文列出相關萃取物、已知成分
- [ ] 科筆記(數十則):該科的物種清單、用途分布、特徵化學
- [ ] 用途 / 部位 / 方法的 MOC
- [ ] Dataview 動態索引,查詢 `_data/kb.duckdb`

**機器生成區與人工撰寫區必須分開:**

```markdown
## 自動生成
<!-- AUTO-START -->
(腳本每次重跑覆寫此區)
<!-- AUTO-END -->

## 我的筆記
(腳本永不觸碰此區)
```

> [!tip] 科筆記是本專案最有價值的產出
> 這是你累積「這一科的特徵化學、萃取注意事項、實務經驗」的地方。機器只能給你結構,知識要自己長。

---

### Phase 7 — AI 問答層(1–2 天)

**目標:** 讓模型透過工具查詢,而不是把資料塞進 context。

**四層混合檢索,各司其職:**

| 層 | 用途 | 實作 |
| --- | --- | --- |
| 精確查表 | 植物名 / 學名 → ID | Phase 1 的別名表 |
| 結構化篩選 | 條件查詢 | DuckDB SQL |
| 圖遍歷 | 「相關的還有什麼」 | 鄰接表 |
| 向量檢索 | 語意模糊的問題 | 只用於敘述欄位 |

包成一組函式,做成 Claude Code 的 skill 或小型 MCP server。

> [!warning] 不要用純向量 RAG
> 學名系統性命名讓近緣物種的 embedding 極為相似,純向量檢索會自信地取錯物種。名稱查詢必須走字典,不走向量。

---

## 六、日常使用方法

建完之後怎麼用。這一節寫的是 Phase 6、7 交付後的實際操作。

### 6.1 三種使用情境

| 情境 | 進入點 | 典型問題 |
| --- | --- | --- |
| 查詢 — 我知道要找什麼 | Claude Code 問答 | 「哪些萃取物來自唇形科?」 |
| 探索 — 我不知道要找什麼 | Obsidian graph view / 科筆記 | 點開一個科,看它連到哪些用途 |
| 累積 — 我學到了新東西 | 物種筆記、科筆記的人工區 | 把實驗結果、供應商資訊寫進去 |

三者共用同一份資料,但入口不同。查詢走問答層,探索走 Obsidian,累積靠手寫。

### 6.2 Obsidian 端

**Graph view 設定**

預設會很亂,必開篩選。建議存三組 filter:

- `path:20-Family/` — 只看科層級,約數十個節點,看得出全貌
- `tag:#用途/抗氧化` — 單一用途的物種分布
- `path:10-Species/ -tag:#已驗證` — 待處理的物種

Group 設定用顏色區分:科用一色、物種用一色、MOC 用一色。沒有顏色區分的 graph view 沒有可讀性。

**Dataview 動態查詢**

不要手動維護清單,寫在 MOC 裡讓它自己長:

```dataview
TABLE family AS "科", extract_count AS "萃取物數"
FROM "10-Species"
WHERE contains(uses, "抗氧化")
SORT extract_count DESC
```

科筆記裡放這個,自動列出該科所有物種:

```dataview
LIST
FROM "10-Species"
WHERE family = this.family_name
SORT file.name ASC
```

**每則筆記的固定入口**

物種筆記底部放一個連向該科的連結,科筆記底部放 Dataview 列出所有屬。這樣任何一個節點都能三步內走到全域。

### 6.3 Claude Code 端

**Slash commands** 放在 `.claude/commands/`,常用的四個:

| 指令 | 檔案 | 功能 |
| --- | --- | --- |
| `/gap` | `gap.md` | 跑空白格推薦,列出某用途下尚未收錄的同科物種 |
| `/species` | `species.md` | 查單一物種:分類、萃取物、已知成分、相關物種 |
| `/audit` | `audit.md` | 檢查斷掉的 wikilink、缺 family 的物種、孤兒筆記 |
| `/refresh` | `refresh.md` | 重跑腳本更新 AUTO 區塊 |

`gap.md` 範例:

```markdown
針對用途「$ARGUMENTS」執行空白格分析:
1. 查 kb.duckdb,統計該用途的萃取物集中在哪些科
2. 對前 5 大科,列出 LOTUS/GBIF 中該科的物種,扣除已收錄者
3. 依該科在此用途的密度排序,輸出候選清單
4. 寫入 90-Reports/gap-$ARGUMENTS.md,不要修改其他檔案
```

**破壞性操作的規矩**

任何批次改寫先 dry-run。這句話寫進 CLAUDE.md 還不夠,實際使用時要養成習慣:

```
把所有 10-Species/ 的 family 欄位重新對照 taxonomy.csv,
先輸出「檔名 / 現值 / 新值」的變更表給我看,不要動檔案。
```

確認後才說「執行剛才的變更」。

### 6.4 問答層的呼叫方式

四層檢索包成四個工具,模型自己決定用哪個。使用者不需要知道差別,但你要知道它該走哪條路,才能判斷答案可不可信:

| 問題 | 應走的路徑 | 若走錯會怎樣 |
| --- | --- | --- |
| 「薄荷的萃取物有哪些?」 | 查表 → SQL | 走向量會抓到胡椒薄荷、貓薄荷等近緣種 |
| 「唇形科常見的用途?」 | SQL 聚合 | — |
| 「跟迷迭香相關的還有什麼?」 | 圖遍歷 | — |
| 「有沒有帶清涼感的萃取物?」 | 向量檢索敘述欄位 | — |

**驗證習慣:** 答案若牽涉具體物種名稱或數量,要求模型附上查詢語句與筆數。看到它憑印象回答學名就是警訊。

### 6.5 增量更新流程

新資料進來時的固定順序,不要跳步:

1. `git commit` 現況
2. 新 CSV 進 `_data/`,跑 Phase 1 抽取 → 只處理別名表中沒有的新字串
3. 新學名經 GBIF 驗證,人工確認後併入 `species_alias.csv`
4. 重跑 Phase 2–4 腳本,更新 DuckDB
5. 跑 `/refresh` 更新所有 AUTO 區塊
6. 跑 `/audit` 檢查有無斷鏈
7. `git diff` 確認人工區塊沒被動到,再 commit

第 7 步是保險。腳本寫錯覆蓋掉半年筆記的事情,發生一次就夠痛。

### 6.6 維護節奏

- **每次新增資料** — 走 6.5 流程
- **每月** — 跑一次 `/audit`,清孤兒筆記與斷鏈
- **每季** — 重下載 LOTUS 與 GBIF,分類系統會更新(APG 分類法時有調整),重跑 Phase 2、4
- **每半年** — 檢視科筆記的人工區,把散落的心得整併成結論

> [!tip] 判斷這個知識庫有沒有活著
> 看科筆記的人工區有沒有在長。如果三個月後那裡還是空的,代表你只是建了一個資料庫,不是知識庫——這時該檢討的是查詢入口不夠順手,而不是再加更多自動化。

---

## 七、時程

| Phase | 內容 | 工作天 |
| --- | --- | --- |
| 0 | 資料剖析 | 0.5 |
| 1 | 學名正規化 ⭐ | 2–3 |
| 2 | 分類階層 | 0.5 |
| 3 | 萃取條件抽取 | 0.5 |
| 4 | LOTUS 富集 | 1 |
| 5 | 挖掘隱藏關聯 | 1–2 |
| 6 | Vault 生成 | 1 |
| 7 | AI 問答層 | 1–2 |
| | **合計** | **7.5–10.5** |

Phase 0–2 是必做骨幹。Phase 4 之後可平行進行。

---

## 八、風險清單

| 風險 | 影響 | 對策 |
| --- | --- | --- |
| 中文俗名一名多物種 | 圖譜關聯錯誤 | 高頻項目人工審核,無法確定者標記隔離 |
| LLM 幻覺出假學名 | 資料污染 | 一律經 GBIF 驗證,不採信未驗證結果 |
| 同物異名未統一 | 節點分裂 | 一律使用 accepted name |
| LOTUS 覆蓋率不均 | 結論偏向熱門物種 | 「無資料」與「無成分」分開統計 |
| 分類系統更新(APG) | 科的歸屬改變 | 記錄 GBIF 擷取日期,定期重跑 |
| 一萬則筆記淹沒 vault | 圖譜不可讀 | 萃取物留資料庫,筆記只寫聚合層級 |
| 腳本覆蓋人工筆記 | 知識遺失 | AUTO 區塊標記 + git commit |

---

## 附錄:CLAUDE.md 範本

```markdown
# Vault 規範

## 目錄
- `_data/` 資料檔,腳本讀寫,不生成筆記
- `10-Species/` 物種筆記,檔名為接受學名
- `20-Family/` 科筆記
- `30-MOC/` 用途、部位、方法的樞紐頁
- `90-Reports/` 分析產出

## 筆記格式
---
tags: [科/xxx]
scientific_name: Genus species
gbif_key: 1234567
family: Xxxaceae
source_updated: YYYY-MM-DD
---

## 撰寫規則
- 物種筆記檔名一律使用 GBIF accepted name,不使用俗名
- 標題用完整句子陳述論點,不用名詞短語
- 只連結真實存在的筆記,連結前先確認檔案存在
- 不得修改 `<!-- AUTO-START -->` 與 `<!-- AUTO-END -->` 之外的內容
- 引用外部資料庫的內容必須標明來源與擷取日期

## 禁止事項
- 不要為單筆萃取物建立筆記
- 不要在未經 GBIF 驗證的情況下寫入學名
- 批次改寫前必須先輸出變更清單供確認,不得直接執行
```

---

## 九、下一步

1. 執行 Phase 0 的資料剖析
2. 抽樣 100 筆敘述,歸納生物來源的表達句型
3. 依句型設計 Phase 1 的抽取 prompt
